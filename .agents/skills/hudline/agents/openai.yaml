interface:
  display_name: "HUD Timeline"
  short_description: "Render HUD timelines and matching mixlines"
  default_prompt: "Use $hudline to render and publicly publish the complete DEBUG HUD timeline, then create and publicly publish its matching $mixline."
